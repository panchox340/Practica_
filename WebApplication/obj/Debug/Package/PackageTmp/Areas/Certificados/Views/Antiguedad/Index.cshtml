@using WebApplication.Extensions

<section class="content-header">
    <div class="callout callout-info" style="margin-bottom: 0 !important;">
        <h4>
            <i class="fa fa-file"></i> Certificado de Antiguedad
        </h4>
        </div>
</section>
    <section class="invoice">
        <div class="box box-default direct-chat direct-chat-primary">
            Estimado @User.Identity.GetNombre()
            <br />
            <br />
            Según nuestros registro su fecha de ingreso a la empresa  @Html.Raw(ViewBag.Empresa) es el  @Html.Raw(ViewBag.fechaIngreso)
            <br />
            <br />

            @*<button type="button" class="btn btn-block btn-primary btn-lg" style="width:30%"  onclick="location.href='@Url.Action("Get_PDF", "Antiguedad")'">Generar</button>*@
            <a href="@Url.Action("PDF","Antiguedad", new {Area = "Certificados" })" class="btn btn-default modal-ajax-load pdf-size" data-toggle="modal" data-target="#myModal" data-remote="false">Generar</a>
   </div>
</section>
